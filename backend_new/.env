PORT=5000
MONGO_URI=mongodb://localhost:27017/rewear
JWT_SECRET=rewear_jwt_secret_token_key_987654321
UPLOADS_DIR=uploads
NODE_ENV=development
# Set to your deployed frontend's exact origin in production to lock down CORS,
# e.g. FRONTEND_URL=https://rewear.example.com — also disables the
# POST /api/auth/switch-persona dev-only endpoint when NODE_ENV=production.
FRONTEND_URL=http://127.0.0.1:5500
